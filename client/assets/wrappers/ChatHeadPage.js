import styled from "styled-components"

const Wrapper = styled.main`
	.chatSmthIForgetName {
		box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
		transition: 0.3s;
	}

	.chatSmthIForgetName:hover {
		box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
	}

	.chatPersonDetails {
		padding: 1px 3px;
		text-align: center;
	}

	.chatPersonDetails p {
		margin-bottom: 0;
	}
`

export default Wrapper
